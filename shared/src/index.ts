/**
 * Re-export barrel for @gym/shared. Task 0 scaffold only, empty but a valid module.
 *
 * Later tasks add the real files here, per spec section 4:
 *   - scenario.ts: RunContext, ScenarioDef, Step, Fault, Assertion, RequestMatcher (Task 2).
 *   - events.ts: RequestEvent, TrainerEvent (Task 1).
 *   - api.ts: trainer HTTP request/response types (Task 1).
 * Each new file gets an `export * from './file.js'` line added here.
 */
export {};
